package com.tpg.par.web.components;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Button extends WebPageComponent {
    Button(WebDriver webDriver, By by) {
        super(webDriver, by);
    }
}
