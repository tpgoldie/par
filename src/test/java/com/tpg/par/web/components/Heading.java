package com.tpg.par.web.components;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Heading extends WebPageComponent {

    public Heading(WebDriver webDriver, By id) {
        super(webDriver, id);
    }
}
